in the console you get help on some method, examples:

dir(str)

help(str.capitalize)

>> import builtilns dir(builtilns)

the open function by default assigns the value 'r' to the arguments
"with open", when you need to read a file.

